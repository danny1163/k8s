apiVersion: v1
kind: Pod 
metadata:
    name : my-prod
    labels: 
      env: UAT-env
      app: my-pod
    namespace: uat 
spec: 
  containers: 
  - name: my-prod
    image: httpd
    ports: 
     -  name: http
        containerPort: 80
  - name: my-prod
    image: tomcat
    ports: 
     -  name: http
        containerPort: 80      